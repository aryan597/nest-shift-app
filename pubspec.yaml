name: nestshift_flutter
description: "NestShift — Smart Home Controller for NestShift OS"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  dio: ^5.9.2
  web_socket_channel: ^3.0.3
  google_fonts: ^8.0.2
  flutter_animate: ^4.5.2
  flutter_secure_storage: ^10.0.0
  mobile_scanner: 7.2.0
  fl_chart: ^1.2.0
  qr_flutter: ^4.1.0
  go_router: ^17.1.0
  shared_preferences: ^2.2.2
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
